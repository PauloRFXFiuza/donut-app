import Logo from "../Assets/Logo.svg"

const Navbar = () => {
    return(
        <nav>
            <div>
                <img src={Logo} alt="Donut logo" />
            </div>
        </nav>

    )
}

export default Navbar